enum EventStatus { upcoming, current, past }
enum EventCategory { personal, birthday, wedding, graduation, other }
enum GiftStatus { available, pledged, purchased }
enum GiftCategory { electronics, books, clothing, other }